---
import Page from '../layouts/Page.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<Page title="Nothing to scoop here" eyebrow="404">
  <p>That page doesn't exist. <a href={`${base}/`}>Back to the home page</a>.</p>
</Page>
